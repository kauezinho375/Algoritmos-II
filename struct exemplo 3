// Ler uma data
#include <stdio.h>

struct Data
{
    int dia;
    int mes;
    int ano;
};

int main()
{
    struct Data d;

    printf("Digite a data ( dia mes ano): ");
    scanf("%d %d %d", &d.dia, &d.mes, &d.ano);

    printf("Data: %02d/%02d/%02d\n", d.dia, d.mes, d.ano);

    return 0;
}
